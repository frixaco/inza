# Inza Web

Web app built with React 19 and Vite.

From the repo root:

```sh
bun run web
bun run web:build
```

## Commands

```sh
bun run dev
bun run build
bun run preview
```

This app is currently a Kaishi Open Deck stress-test viewer. It expects a deck
to be available at `public/deck`.
