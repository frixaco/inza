{
  "name": "inza",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run format:check && bun run lint && bun run test && bun run typecheck",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "parse": "bun stuff/index.ts",
    "test": "bun test stuff",
    "typecheck": "bun x tsc --noEmit --allowImportingTsExtensions --forceConsistentCasingInFileNames --module Preserve --moduleResolution Bundler --skipLibCheck --strict --target ES2022 --types bun stuff/*.ts"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0",
    "oxlint-tailwindcss": "^1.2.0"
  },
  "packageManager": "bun@1.4.0"
}
